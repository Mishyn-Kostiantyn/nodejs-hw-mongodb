description: 'Forbidden'
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 403
        message:
          type: string
          example: 'Contact with this Id does not belong to yours contacts!'
